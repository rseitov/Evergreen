server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: serve index.html for any client-side route (e.g. /guides/:id, /share/:token).
    location / {
        try_files $uri $uri/ /index.html;
    }
}
